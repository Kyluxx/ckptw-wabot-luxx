import { fireConfig, fireData, AuthenticationState } from "../Types";
export declare const useFireAuthState: (config: fireConfig) => Promise<{
    state: AuthenticationState;
    saveCreds: () => Promise<void>;
    clear: () => Promise<void>;
    removeCreds: () => Promise<void>;
    query: (collection: string, docId: string) => Promise<fireData>;
}>;
